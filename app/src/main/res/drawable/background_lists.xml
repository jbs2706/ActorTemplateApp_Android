<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle" >
    <!-- This is the stroke you want to define -->
    <stroke android:width="2dp"
        android:color="@color/colorAccent"/>

    <!-- Optional, round your corners -->
    <corners android:bottomLeftRadius="7dp"
        android:topLeftRadius="6dp"
        android:bottomRightRadius="6dp"
        android:topRightRadius="7dp" />

    <!-- Optional, fill the rest of your background with a color or gradient, use transparent if you only want the border to be displayed-->
    <gradient android:startColor="@color/itemColor"
        android:endColor="@color/itemColor"
        android:angle="90"/>

</shape>
